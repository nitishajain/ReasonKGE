# Configuration examples

These files are archived Yago3-10/TransE configuration snapshots:

- `transe-baseline.yaml` — standard negative-sampling baseline
- `transe-reasonkge-iteration2.yaml` — ReasonKGE training to 200 epochs
- `transe-reasonkge-iteration3.yaml` — ReasonKGE training to 300 epochs

They are provided for reference only.
